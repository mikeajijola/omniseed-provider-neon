{
  "name": "@omniseed/provider-neon",
  "version": "0.1.0-alpha.1",
  "description": "Neon Provider for OmniSeed durable runtime-state memory resources",
  "private": true,
  "scripts": {
    "test": "python3 -m unittest discover -s test -p 'test_*.py' -v"
  },
  "engines": { "node": ">=22" },
  "license": "Apache-2.0"
}
